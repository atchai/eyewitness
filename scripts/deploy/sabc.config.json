{
	"eyewitness-app": {
		"containerDefinitions": [{
			"name": "eyewitness-app",
			"image": "538881967423.dkr.ecr.eu-west-1.amazonaws.com/eyewitness-app:latest",
			"memoryReservation": 128,
			"essential": true,
			"portMappings": [{
				"containerPort": 5000,
				"hostPort": 5000,
				"protocol": "tcp"
			}],
			"environment": [{
				"name": "NODE_ENV",
				"value": "{{REPLACE_ME}}"
			}, {
				"name": "PROVIDER_ID",
				"value": "sabc"
			}, {
				"name": "ENTRY_POINT",
				"value": "app"
			}],
			"logConfiguration": {
				"logDriver": "awslogs",
				"options": {
					"awslogs-region": "{{REPLACE_ME}}",
					"awslogs-group": "{{REPLACE_ME}}",
					"awslogs-stream-prefix": "eyewitness-app"
				}
			}
		}]
	},
	"eyewitness-read-server": {
		"containerDefinitions": [{
			"name": "eyewitness-read-server",
			"image": "538881967423.dkr.ecr.eu-west-1.amazonaws.com/eyewitness-app:latest",
			"memoryReservation": 128,
			"essential": true,
			"portMappings": [{
				"containerPort": 6000,
				"hostPort": 6000,
				"protocol": "tcp"
			}],
			"environment": [{
				"name": "NODE_ENV",
				"value": "{{REPLACE_ME}}"
			}, {
				"name": "PROVIDER_ID",
				"value": "sabc"
			}, {
				"name": "ENTRY_POINT",
				"value": "read-server"
			}],
			"logConfiguration": {
				"logDriver": "awslogs",
				"options": {
					"awslogs-region": "{{REPLACE_ME}}",
					"awslogs-group": "{{REPLACE_ME}}",
					"awslogs-stream-prefix": "eyewitness-read-server"
				}
			}
		}]
	}
}
